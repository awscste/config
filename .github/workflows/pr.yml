name: Opus Agent Full Repo PR Code Analysis

on:
  pull_request:
    types: [opened, synchronize, reopened]
    branches:
      - main

jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Install Opus Agent
        run: |
          curl -fsSL https://raw.githubusercontent.com/opencode-ai/opencode/refs/heads/main/install | bash
          source /home/runner/.bashrc
          echo "ls -la /home/runner/.opencode/bin: " ls -la /home/runner/.opencode/bin
          /home/runner/.opencode/bin
          env
          which opencode
          opencode --help

      - name: Run Opus Agent on PR (Full Repo)
        run: |
          opencode -p "review code" -d -c .
